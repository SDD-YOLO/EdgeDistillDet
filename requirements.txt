# 与 pyproject.toml [project.dependencies] 保持一致；修改时请同步两处。
# 安装：pip install -r requirements.txt
# 开发依赖（pytest 等）：pip install -e ".[dev]"
# CUDA 版 PyTorch：可先按官方说明安装 torch/torchvision，再执行本文件。

# === 核心依赖（必须）===
torch==2.5.1
torchvision==0.20.1
ultralytics==8.3.75
onnx==1.17.0
onnxruntime==1.20.1
onnxslim==0.1.91
opencv-python==4.11.0.86
numpy==1.26.4
pandas==2.2.3
matplotlib==3.10.0
pyyaml==6.0.2
tqdm==4.67.1
fastapi==0.115.6
pydantic==2.10.6
pydantic-settings==2.7.1
uvicorn[standard]==0.32.1
requests==2.32.3
psutil==6.1.1

# === 可选：日志 / 监控 ===
wandb==0.19.8

# === 可选：LLM ===
langgraph==0.2.76
langchain-core==0.3.33

# Optional heavy runtimes (moved to pyproject optional-dependencies)
# Install extras when needed, e.g. `pip install -e .[tf]`, `pip install -e .[edge]`, `pip install -e .[ml]`


-e .




# Optional model conversion dependencies for ONNX/CoreML/TFLite/SavedModel and other Ultralytics export targets
# Install with: pip install -r requirements-export.txt

# Development / linting (install via extras or dev requirements)
# pip install ruff mypy black pre-commit
